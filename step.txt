🔹 Step 1: Upload and Extract Dataset
Upload the GTZAN ZIP file (or folder containing .au files organized by genre).
Extract all files to a working directory in Colab.

🔹 Step 2: Feature Extraction Loop through .au files.
Extract features using librosa (e.g., MFCCs).
Store features and labels (genre) in a pandas DataFrame.
(Optional) Save the features as a .csv file for quick reuse.

🔹 Step 3: Load Data for ML
If CSV exists, load it; else use the extracted DataFrame.
Separate features (X) and labels (y).
Encode the genre labels.

🔹 Step 4: Train/Test Split
Split the dataset into training and test sets using train_test_split.

🔹 Step 5: Build ML Pipelines
Use Pipeline from sklearn to chain:
StandardScaler for normalization
ML classifier (Logistic Regression, KNN, Random Forest)

🔹 Step 6: Train Models
Fit models on the training data.

🔹 Step 7: Evaluate Models
Predict on test data.
Show accuracy, classification report, and confusion matrix (with seaborn heatmap).

🔹 Step 8: Ensemble Method (VotingClassifier)
Combine models in a VotingClassifier and evaluate.

🔹 Step 9: Save Best Model
Use joblib to save the trained model for future deployment.

🔹 Step 10: Deploy Interface (Later)
Use Gradio or Streamlit to create a simple web interface (not covered now).
